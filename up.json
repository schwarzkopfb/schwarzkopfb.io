{
  "name": "schwarzkopfb-io",
  "profile": "default",
  "regions": [
    "eu-west-3"
  ],
  "stages": {
    "staging": {
      "domain": "beta.schwarzkopfb.io"
    }
  },
  "hooks": {
    "build": "# disable auto-fallback to `npm run build`, because --no-build cli arg is ignored in apex/actions/up"
  }
}